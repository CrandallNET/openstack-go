usage: openstack server migration abort [-h] <server> <migration>

Cancel an ongoing live migration. This command requires ``--os-compute-api-
version`` 2.24 or greater.

positional arguments:
  <server>     Server (name or ID)
  <migration>  Migration (ID)

options:
  -h, --help   show this help message and exit
