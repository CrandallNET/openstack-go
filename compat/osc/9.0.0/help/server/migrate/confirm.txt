usage: openstack server migrate confirm [-h] <server>

DEPRECATED: Use 'server migration confirm' instead.

positional arguments:
  <server>    Server (name or ID)

options:
  -h, --help  show this help message and exit
